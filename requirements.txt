# Guppy AI Suite — Python dependencies
# Updated 2026-04-12

# ── Core UI ───────────────────────────────────────────────
PySide6

# ── AI / LLM ──────────────────────────────────────────────
anthropic
openai
google-generativeai
mistralai

# ── Voice (STT + TTS) ─────────────────────────────────────
sounddevice
soundfile
numpy
faster-whisper
kokoro

# ── Web / Scraping ────────────────────────────────────────
requests
beautifulsoup4

# ── Media ─────────────────────────────────────────────────
spotipy
yt-dlp

# ── Gmail API ─────────────────────────────────────────────
google-api-python-client
google-auth-oauthlib

# ── Scheduling / Notifications ────────────────────────────
APScheduler
python-dateutil
win11toast

# ── System / Windows ──────────────────────────────────────
pywin32
psutil
pyperclip
keyboard

# ── Misc ──────────────────────────────────────────────────
SpeechRecognition
ruff
mypy
pytest-xdist
unidiff

# ── Phase 3: Remote Access (FastAPI + Auth) ──────────────
fastapi
uvicorn[standard]
python-jose[cryptography]
httpx

# ── Phase 4: Wake Word Detection (optional) ──────────────
# Install to enable openwakeword fast path (<1% CPU).
# Without this, wake word falls back to transcription-based detection.
# Custom "hey guppy" model: record 10-20 samples, train with openwakeword CLI.
openwakeword

# ── Semantic Memory (optional Chroma backend) ─────────────
# Default semantic backend uses SQLite and does not require chromadb.
# Install chromadb only if GUPPY_SEMANTIC_BACKEND=chroma.
chromadb
