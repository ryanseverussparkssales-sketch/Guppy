# Guppy development-only dependencies
# Install with: python -m pip install -r requirements-dev.txt

-r requirements.txt

# Security and quality extras
ruff
mypy
pytest-cov
pytest-xdist
unidiff
pip-audit
