# Guppy — Session Progress Report
**Date:** 2026-04-24  
**Against:** Q2 Roadmap (`docs/archive/2026-04/ROADMAP_2026_Q2_EXECUTION_TRANCHES.md`) and Goals (`docs/archive/reference-history/GOALS.md`)

---

## Roadmap Tranche Status

| Tranche | Target | Status | Notes |
|---------|--------|--------|-------|
| **T1 – Stability & Hardening** | May 1–14 | 🟢 ~85% done | Completed ahead of schedule in this session |
| **T2 – Web UI Polish** | May 8–21 | 🟡 ~60% done | Chat, settings, tools wired; stubs remain |
| **T3 – Voice I/O** | May 15–28 | 🔴 ~10% done | useVoice hook exists; no real STT/TTS pipeline |
| **T4 – Tools API** | May 22–Jun 4 | 🟢 ~70% done | REST endpoints done; no AI function-calling integration |
| **T5 – Credentials** | May 20–Jun 6 | 🟡 ~40% done | Store/delete/list endpoints working; no encryption at rest |
| **T6 – Admin Panel** | Jun 3–16 | 🟡 ~30% done | AdminPanel view exists with repair/logs tabs; no live data |
| **T7 – Desktop Launcher** | Jun 10–23 | 🟡 ~20% done | Fishbowl companion added; Qt launcher stable; no auto-update |

---

## What Was Completed This Session (2026-04-24)

### Bug fixes
- `routes_settings.py:168` — extra arg in `set_active_provider()` call (would crash on provider switch)
- `store/index.ts` — `Provider` type not re-exported (TS error in SettingsView)
- `hooks/useSettings.ts` — duplicate `Provider` type removed, now imports from store
- `realtime_inference_support.py:85` — boolean precedence bug `not x in set` → `not (x in set)` (inverted Semantic RAG)
- 86× `del user_id` anti-pattern across 13 route files → renamed to `_user_id`
- `cli/launch.py` — `os.environ["GUPPY_JWT_SECRET"]` KeyError risk on missing key

### New API surface
- `src/guppy/api/routes_tools.py` — full tools management: GET /tools, POST /tools/:id/enable|disable, SQLite-backed, seeded with 8 tools
- Web UI toggle wired end-to-end with optimistic spinner + Sonner toast

### New tooling / infrastructure
| Tool | File | Impact |
|------|------|--------|
| pydantic-settings | `src/guppy/config.py` | All env vars now typed + validated in one place |
| loguru | `src/guppy/logging_setup.py` | Structured logging; intercepts stdlib for uvicorn/FastAPI |
| rich | `src/guppy/cli/launch.py` | Coloured [launch] output with green/yellow/red levels |
| Alembic + SQLAlchemy | `migrations/` | Schema migrations; `alembic upgrade head` applies `0001_initial_schema` |
| react-markdown + shiki | `web/src/components/chat/MarkdownMessage.tsx` | Markdown + syntax-highlighted code in chat |
| Sonner | `web/src/App.tsx` | Toast notifications wired to settings saves, tool toggles |
| framer-motion | `web/src/views/AssistantView.tsx` | Chat bubbles animate in |
| date-fns | `web/src/views/AssistantView.tsx` | Consistent HH:mm timestamp formatting |
| react-hotkeys-hook | `web/src/components/layout/AppShell.tsx` | Ctrl+K / Escape replace manual addEventListener |
| pytest-asyncio | `pytest.ini` | `asyncio_mode = auto` — async tests need no decorator |
| Vitest | `web/vite.config.ts` | Component test runner; 5 MarkdownMessage tests passing |
| Playwright | `web/playwright.config.ts` | E2E scaffold ready; no tests written yet |

### New surfaces
- `src/guppy/apps/fishbowl_app.py` — always-on-top PySide6 companion widget with animated fish, chat panel, Ctrl+Space hotkey
- `guppy_fishbowl.py` — root shim for CLI launch
- `Desktop\Guppy\Fishbowl.lnk` — one-click shortcut

### Button audit
57 buttons fully wired, 18 stubs identified. Priority stubs:
- New Instance / Create Instance (API exists, onClick missing)
- Command palette: Initialize Node, Start Node, Halt All Nodes (console.log stubs)
- LibraryView: copy, edit, delete on items

---

## State vs Goals

### Product North Star ("personal, persistent, calm")
| Goal | Status |
|------|--------|
| One clear place | ✅ Web UI with consistent navigation |
| Continuity (conversation persistence) | ✅ Chat history in SQLite |
| Low-friction daily use | 🟡 Works but stubs visible; voice not ready |
| Calm workspace | 🟡 Toast notifications clean; some views have dead buttons |
| Trustworthy / recoverable | ✅ Repair token, JWT, audit trail |

### Measurable targets (from GOALS.md)
| Target | Current | Goal |
|--------|---------|------|
| Time to first use | ~5 min (venv + Ollama) | < 10 min ✅ |
| First AI response latency | Depends on Ollama model | < 3 sec 🟡 |
| Voice interruption success | N/A — no voice pipeline | 100% 🔴 |
| Pilot gates pass | Not run recently | Mandatory 🟡 |
| Frontend test coverage | 5 tests (MarkdownMessage) | > 95% 🔴 |

---

## Remaining Work (Priority Order)

### 🔴 Must-do before T1 closes
1. **Wire 3 Command Palette stubs** — Initialize Node, Start Node, Halt All Nodes → POST to `/api/instances`
2. **Wire New Instance button** — InstancesView "+ New Instance" onClick
3. **Run pilot gates** — `python tools/dev_workflow.py release-check`

### 🟡 T2 Web UI polish
4. LibraryView item actions (copy, edit, delete on hover)
5. TopBar notifications + settings buttons
6. Dashboard quick-action shortcuts
7. Voices view "Refresh voices" button

### 🟡 T4 Tools — AI integration
8. Wire tool definitions into Claude/Ollama function-calling flow
9. Tool execution logging

### 🔴 T3 Voice (not started)
10. STT pipeline (Whisper or browser Web Speech API)
11. TTS pipeline (ElevenLabs or system TTS)
12. Voice interruption handling

### 🟡 Infrastructure
13. Migrate remaining `os.environ.get("GUPPY_*")` in `server_runtime.py` / `auth.py` to `settings`
14. Write Playwright E2E test for chat happy path
15. Expand Vitest coverage beyond MarkdownMessage

---

## Tech Debt Added This Session
- `useSettings` in `useApi.ts` is now dead code (same name exists in dedicated `useSettings.ts`). Should be removed or renamed.
- Fishbowl app not yet tested end-to-end (animation, chat, hotkey).
- Alembic manages `guppy_main.db` but individual routes still create their own `.db` files. Consolidation needed.
