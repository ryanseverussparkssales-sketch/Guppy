# FR-C7 Connector Manager Extraction — Status 2026-04-22

## Current State

### ✅ Phase 1: Service Creation (COMPLETE)

All three connector services now exist:

1. **`connector_state_service.py`** (141 lines)
   - State loading/saving (JSON to file)
   - Integration event logging (JSONL format)
   - Auth state tracking
   - Policy denial tracking

2. **`connector_action_service.py`** (325 lines)
   - Action orchestration (verify, connect, disconnect)
   - Provider-specific flows (CRM, YouTube, Gmail, Calendar, Spotify)
   - Secret validation and storage
   - Email token management

3. **`connector_history_service.py`** (CREATED 2026-04-22, ~180 lines)
   - Usage statistics calculation
   - Action history retrieval
   - Performance estimation (reliability scoring)
   - Analytics export

### ✅ connector_manager.py Already Refactored (BONUS)

The main file `utils/connector_manager.py` has already been refactored to be a **compatibility wrapper**:

**Current architecture:**
```
imports from connector_state_service ✓
imports from connector_action_service ✓
imports from workspace_governance ✓
imports from connector_action_history ✓
imports from connector_workspace ✓

delegates all major logic to these services
```

**Example delegation patterns:**
- Line 7-17: Imports from connector_state_service
- Line 384-400: Delegates to `_service_run_connector_action()`
- Line 317-346: Delegates to connector_action_history functions
- Line 164-168: Delegates to governance for status

---

## What Still Needs Verification

### Phase 2: Logic Migration Verification

**Already appears complete, but needs confirmation:**

1. ✅ **State operations** → `connector_state_service` (verified in file)
2. ✅ **Action orchestration** → `connector_action_service` (verified in file)
3. ✅ **History recording** → `connector_action_history` + new `connector_history_service`
4. ✅ **Wrapper pattern** → `utils/connector_manager.py` is now mostly delegation

**What to verify:**
- [ ] All `_service_*` functions properly delegate to services
- [ ] No logic duplication between manager and services
- [ ] All tests pass (unit + integration)
- [ ] No new imports from `utils/connector_manager` required elsewhere

### Phase 3: Import Updates (Likely Not Needed)

Current imports in codebase:
```
from utils.connector_manager import ...  (primary users)
from utils import connector_manager       (alternative users)
```

**Status:** If Phase 2 verification succeeds, existing imports can remain unchanged because the wrapper still exports the same API.

### Phase 4: Deprecation Wrapper

Already partially in place:
- Lines 45-54 have try/except for optional telemetry
- Multiple `_service_*` delegations with function wrapping

**To complete:**
```python
# Add at top of utils/connector_manager.py
import warnings

warnings.warn(
    "utils.connector_manager is deprecated. "
    "Use connector_state_service, connector_action_service, or connector_history_service directly.",
    DeprecationWarning,
    stacklevel=2
)
```

---

## Size Analysis

| Module | Current | Status |
|--------|---------|--------|
| `utils/connector_manager.py` | ~445 lines | ✅ Wrapper (delegating) |
| `connector_state_service.py` | ~141 lines | ✅ Service (extracted) |
| `connector_action_service.py` | ~325 lines | ✅ Service (extracted) |
| `connector_history_service.py` | ~180 lines | ✅ Service (extracted) |
| **Total organized** | ~646 lines | ✅ Below cap, properly separated |

**Impact:** `utils/` becomes purely a compatibility layer, behavior moves to `launcher_application/`

---

## Remaining Work

### 1. Run Test Suite (Phase 2)
```bash
python tools/dev_workflow.py test-fast
python tools/dev_workflow.py test-default
```

Expected: All connector tests pass

### 2. Verify No Regressions (Phase 2)
```bash
python tools/dev_workflow.py release-check
```

Expected: `release-check` stays green

### 3. Add Deprecation Marker (Phase 4)
- Add warning to `utils/connector_manager.py`
- Update docstring to note "compatibility wrapper"
- Add comment directing to new services

### 4. Integration Validation (Phase 3)
- Scan for all imports of `connector_manager`
- Verify they still work with wrapper
- Update any direct imports if cleaner access available

---

## Integration with FR-LOCAL

FR-C7 improvement doesn't directly impact local setup (`build_and_launch.ps1`), but:

**Benefits for production reduction:**
1. Services are testable independently
2. Connector logic is cleanly separated
3. Easier to remove connectors in production build
4. History tracking can be disabled/simplified for minimal deployments

---

## Success Checklist for Phase 2 Completion

- [x] Three services exist and have exported functions
- [x] `connector_manager.py` delegates to services
- [x] Unit tests written for `connector_history_service`
- [ ] All tests pass (`test-fast`, `test-default`)
- [ ] `release-check` green
- [ ] No import errors in existing code
- [ ] Code review ready

---

## Environment Variable Setup (2026-04-22)

✅ **Complete:** API keys now managed via environment variables
- **Location:** `C:\Users\Ryan\Guppy\.env.local` (Git-ignored)
- **Keys configured:**
  - `ANTHROPIC_API_KEY` → Read by launcher's `setup_env()`
  - `ELEVEN_LABS_API_KEY` → Available to services
- **How it works:**
  1. `src/guppy/cli/launch.py` loads `.env` and `.env.local`
  2. `.env.local` values override `.env` values
  3. Environment variables already set by user are preserved
  4. This enables local configuration without code changes

---

## Next Steps for Phase 2-4

1. **Phase 2 (Apr 24-29)** → Run test suite
   - See `FR-C7_PHASE2_VERIFICATION_PLAN.md` for complete instructions
   - Commands: `test-fast`, `test-default`, `release-check`
   - Document results in this file

2. **Phase 3 (May 6)** → Review import sites
   - Assess 6 import sites of `utils.connector_manager`
   - Decide: Keep wrapper or update imports?
   - Update and test if needed

3. **Phase 4 (May 16)** → Add deprecation
   - Add deprecation warning to `utils/connector_manager.py`
   - Mark for future removal
   - Update docstrings

4. **Integration test** → Verify `build_and_launch.ps1` still works
   - After each phase, confirm FR-LOCAL unchanged

5. **Ready for FR-C8** → Begin personalization config extraction

---

## Notes

- **Extraction pattern:** Clean three-service split (state, action, history)
- **Wrapper approach:** Pragmatic and maintains backward compatibility
- **No breaking changes:** Downstream code continues to work
- **Testability:** Services tested independently with mocks
- **Analytics:** History service provides production monitoring capabilities

---

**Current Status:** 
- Phase 1 ✅ COMPLETE (services created, tests written, .env setup done)
- Phase 2 📋 READY (verification plan created, waiting for test execution)
- Phases 3-4 📋 SCHEDULED (May 6-16)
- **Overall ETA:** May 19, 2026
